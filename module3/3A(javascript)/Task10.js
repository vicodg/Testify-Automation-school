function calculateAreaOfRectangle(length, width) {
    let area = length * width;
    return area;
}
let length = 15;
let width = 23;
let area = calculateAreaOfRectangle(length, width);
console.log("The area of the rectangle is: " + area);
